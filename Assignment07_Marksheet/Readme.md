# Assignment 7 – Marksheet Table

## Objective

Create a Marksheet table and retrieve records using WHERE and ORDER BY.

## Problem Statement

Create a Marksheet table and insert the given sample records.

Display students whose marks are greater than 80 and sort the result in descending order of marks.

## Expected Outcome

Only students scoring above 80 should be displayed in descending order.